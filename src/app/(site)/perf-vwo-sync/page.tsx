import Home from "@/components/Home";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "NextCommerce | Perf VWO Sync",
  description: "Perf VWO Sync variant of the NextCommerce homepage.",
};

export default function PerfVwoSyncPage() {
  return <Home />;
}
