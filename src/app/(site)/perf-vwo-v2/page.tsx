import Home from "@/components/Home";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "NextCommerce | Perf VWO V2",
  description: "Perf VWO V2 variant of the NextCommerce homepage.",
};

export default function PerfVwoV2Page() {
  return <Home />;
}
