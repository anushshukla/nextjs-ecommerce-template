import Home from "@/components/Home";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "NextCommerce | Hybrid Mode v6",
  description: "Hybrid mode v6 variant of the NextCommerce homepage.",
};

export default function HybridModeV6Page() {
  return <Home />;
}
