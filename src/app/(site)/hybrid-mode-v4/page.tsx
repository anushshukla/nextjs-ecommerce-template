import Home from "@/components/Home";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "NextCommerce | Hybrid Mode v4",
  description: "Hybrid mode v4 variant of the NextCommerce homepage.",
};

export default function HybridModeV4Page() {
  return <Home />;
}
