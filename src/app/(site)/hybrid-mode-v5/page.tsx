import Home from "@/components/Home";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "NextCommerce | Hybrid Mode v5",
  description: "Hybrid mode v5 variant of the NextCommerce homepage.",
};

export default function HybridModeV5Page() {
  return <Home />;
}
